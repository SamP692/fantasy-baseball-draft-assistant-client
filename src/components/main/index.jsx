/* Core Libraries */
import { h } from "preact"

/* Styles */
import "./main.css"

/* Main */
function Main({ children }) {
    return (
        <main>
            {children}
        </main>
    )
}

export default Main
